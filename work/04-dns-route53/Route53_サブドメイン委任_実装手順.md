# Route53 サブドメイン委任 実装手順書

## 📅 作業情報
- **実施日**: 2025-12-23
- **対象ドメイン**: hijiri0404.link
- **委任サブドメイン**: sub.hijiri0404.link
- **テストレコード**: test.sub.hijiri0404.link

---

## 🎯 実装概要

親ドメイン `hijiri0404.link` から `sub.hijiri0404.link` を別のRoute53ホストゾーンに委任し、独立したDNS管理を可能にする設定を実施。

---

## 📋 実装手順

### 1️⃣ 親ドメインのホストゾーン情報確認

```bash
aws route53 list-hosted-zones-by-name --dns-name hijiri0404.link
```

**取得情報:**
- **ホストゾーンID**: `Z05608792OMRUEGE6GF3A`
- **ネームサーバー**:
  - ns-2027.awsdns-61.co.uk
  - ns-150.awsdns-18.com
  - ns-713.awsdns-25.net
  - ns-1209.awsdns-23.org

---

### 2️⃣ サブドメイン用の新しいホストゾーン作成

```bash
aws route53 create-hosted-zone \
  --name sub.hijiri0404.link \
  --caller-reference subdomain-delegation-test-20251223
```

**作成されたホストゾーン:**
- **ホストゾーンID**: `Z04428142DRESCQ1V9IJ2`
- **ネームサーバー**:
  - ns-1864.awsdns-41.co.uk
  - ns-1351.awsdns-40.org
  - ns-50.awsdns-06.com
  - ns-1016.awsdns-63.net

---

### 3️⃣ 親ドメインにNSレコード追加（委任設定）

**JSONファイル作成** (`ns-delegation-change.json`):

```json
{
  "Changes": [
    {
      "Action": "CREATE",
      "ResourceRecordSet": {
        "Name": "sub.hijiri0404.link",
        "Type": "NS",
        "TTL": 300,
        "ResourceRecords": [
          {"Value": "ns-1864.awsdns-41.co.uk"},
          {"Value": "ns-1351.awsdns-40.org"},
          {"Value": "ns-50.awsdns-06.com"},
          {"Value": "ns-1016.awsdns-63.net"}
        ]
      }
    }
  ]
}
```

**NSレコード追加実行:**

```bash
aws route53 change-resource-record-sets \
  --hosted-zone-id Z05608792OMRUEGE6GF3A \
  --change-batch file:///tmp/aws-api-mcp/workdir/ns-delegation-change.json
```

**結果:**
- ChangeID: `/change/C07823463K0IRIP5G2VIO`
- Status: `PENDING` → 通常60秒以内に`INSYNC`に変更

---

### 4️⃣ サブドメインにテスト用Aレコード追加

**JSONファイル作成** (`test-a-record.json`):

```json
{
  "Changes": [
    {
      "Action": "CREATE",
      "ResourceRecordSet": {
        "Name": "test.sub.hijiri0404.link",
        "Type": "A",
        "TTL": 300,
        "ResourceRecords": [
          {"Value": "192.0.2.1"}
        ]
      }
    }
  ]
}
```

**Aレコード追加実行:**

```bash
aws route53 change-resource-record-sets \
  --hosted-zone-id Z04428142DRESCQ1V9IJ2 \
  --change-batch file:///tmp/aws-api-mcp/workdir/test-a-record.json
```

**結果:**
- ChangeID: `/change/C06749053JMO9W194ZMNY`
- Status: `PENDING`

---

### 5️⃣ Zone Apex（sub.hijiri0404.link自体）のAレコード追加

**重要な質問**: `sub.hijiri0404.link` をWebブラウザでHTTP接続したい場合、Aレコードは委任元・委任先のどちらに設定？

**回答**: **委任先（Z04428142DRESCQ1V9IJ2）に設定**

#### DNS委任の仕組み

```
ブラウザ → リゾルバ → ルートサーバー
  → .link TLD
  → hijiri0404.link のネームサーバー
  → 「sub.hijiri0404.linkのNSレコード発見」
  → 委任先ネームサーバー（ns-1864.awsdns-41.co.uk等）に転送
  → 委任先ホストゾーンのAレコードを返す
```

#### なぜ委任先なのか？

1. **NSレコードによる権威の委譲**
   - 親ゾーンにNSレコードを追加した時点で、`sub.hijiri0404.link`およびその配下すべての権威は委任先に移譲

2. **親ゾーンのAレコードは参照されない**
   - NSレコードが存在する場合、そのサブドメインに関するクエリは必ず委任先に転送
   - 親ゾーンにAレコードを追加しても無効

3. **委任先がすべてを管理**
   - Zone Apex（`sub.hijiri0404.link`自体）
   - サブドメイン（`*.sub.hijiri0404.link`）
   - すべてのレコードタイプ（A, AAAA, TXT, MX等）

**JSONファイル作成** (`sub-apex-a-record.json`):

```json
{
  "Changes": [
    {
      "Action": "CREATE",
      "ResourceRecordSet": {
        "Name": "sub.hijiri0404.link",
        "Type": "A",
        "TTL": 300,
        "ResourceRecords": [
          {"Value": "192.0.2.100"}
        ]
      }
    }
  ]
}
```

**Zone Apex Aレコード追加実行:**

```bash
aws route53 change-resource-record-sets \
  --hosted-zone-id Z04428142DRESCQ1V9IJ2 \
  --change-batch file:///tmp/aws-api-mcp/workdir/sub-apex-a-record.json
```

**結果:**
- ChangeID: `/change/C03604512Z43ISFQ9MEDA`
- Status: `PENDING`

---

## ✅ 動作確認

### NSレコード確認
```bash
dig sub.hijiri0404.link NS +short
```

**結果:**
```
ns-1351.awsdns-40.org.
ns-1864.awsdns-41.co.uk.
ns-50.awsdns-06.com.
ns-1016.awsdns-63.net.
```

✅ **4つのネームサーバーが正しく返される**

---

### Aレコード解決確認
```bash
dig test.sub.hijiri0404.link A +short
```

**結果:**
```
192.0.2.1
```

✅ **委任されたホストゾーンのAレコードが正しく解決される**

---

### 委任先ネームサーバーへの直接クエリ
```bash
dig @ns-1864.awsdns-41.co.uk test.sub.hijiri0404.link A +short
```

**結果:**
```
192.0.2.1
```

✅ **委任先のネームサーバーが正しく応答**

---

### Zone Apex（sub.hijiri0404.link）のAレコード確認

```bash
dig sub.hijiri0404.link A +short
```

**結果:**
```
192.0.2.100
```

✅ **委任されたゾーンのApex（頂点）も正しく解決される**

#### 委任先ネームサーバーへの直接クエリ
```bash
dig @ns-1864.awsdns-41.co.uk sub.hijiri0404.link A +short
```

**結果:**
```
192.0.2.100
```

✅ **委任先でZone Apexが管理されていることを確認**

---

## 🔑 重要ポイント

### ✅ 成功要因
1. **NSレコードの正確な設定**
   - 委任先ホストゾーンの全4つのネームサーバーを指定
   - TTLを300秒に設定（テスト目的）

2. **適切なホストゾーン管理**
   - 親ドメイン: 委任用NSレコードのみ追加
   - サブドメイン: 独立したホストゾーンで自由に管理

3. **SOAレコードの扱い**
   - 親ドメイン側には **SOAレコードを追加しない**
   - 委任先が権威サーバーとなるため

4. **Zone Apexのレコード配置**
   - `sub.hijiri0404.link` 自体のAレコード等は **委任先に配置**
   - NSレコードによる委任後は、すべてのレコードが委任先で管理される
   - 親ゾーンに追加したAレコードは参照されない（NSが優先）

### ⚠️ 注意事項

1. **TTL設定**
   - 本番環境では86400秒（24時間）を推奨
   - テスト時は300-900秒で素早い変更テストが可能

2. **DNSプロパゲーション**
   - 変更は通常60秒以内に伝播
   - グローバルな完全伝播には最大48時間かかる場合あり

3. **課金への影響**
   - 新しいホストゾーンの作成により月額$0.50の課金が発生
   - クエリ料金は通常通り

---

## 📊 設定構成図

```
hijiri0404.link (Z05608792OMRUEGE6GF3A)
├── NS: ns-2027.awsdns-61.co.uk
├── NS: ns-150.awsdns-18.com
├── NS: ns-713.awsdns-25.net
├── NS: ns-1209.awsdns-23.org
└── 【委任レコード】
    └── sub.hijiri0404.link NS
        ├── ns-1864.awsdns-41.co.uk
        ├── ns-1351.awsdns-40.org
        ├── ns-50.awsdns-06.com
        └── ns-1016.awsdns-63.net
            ↓ 委任
sub.hijiri0404.link (Z04428142DRESCQ1V9IJ2)
├── NS: ns-1864.awsdns-41.co.uk
├── NS: ns-1351.awsdns-40.org
├── NS: ns-50.awsdns-06.com
├── NS: ns-1016.awsdns-63.net
├── sub.hijiri0404.link A 192.0.2.100 【Zone Apex】
├── test.sub.hijiri0404.link A 192.0.2.1
├── aaa.sub.hijiri0404.link A 192.0.2.10
└── bbb.sub.hijiri0404.link A 192.0.2.20
```

---

## 🚀 応用例

### ユースケース1: 部門別DNS管理
```
example.com
├── sales.example.com    → 営業部門が独立管理
├── dev.example.com      → 開発部門が独立管理
└── support.example.com  → サポート部門が独立管理
```

### ユースケース2: 環境別分離
```
mycompany.com
├── prod.mycompany.com   → 本番環境（厳格な変更管理）
├── staging.mycompany.com → ステージング環境
└── dev.mycompany.com    → 開発環境（自由な変更）
```

### ユースケース3: 地域別分離
```
global-company.com
├── us.global-company.com     → 米国リージョン
├── eu.global-company.com     → 欧州リージョン
└── apac.global-company.com   → アジア太平洋リージョン
```

---

## 🔧 トラブルシューティング

### NSレコードが返されない
```bash
# キャッシュをバイパスして確認
dig @8.8.8.8 sub.hijiri0404.link NS

# Route53のネームサーバーに直接問い合わせ
dig @ns-2027.awsdns-61.co.uk sub.hijiri0404.link NS
```

### Aレコードが解決されない
```bash
# NSレコードが正しく設定されているか確認
dig sub.hijiri0404.link NS

# 委任先ネームサーバーに直接クエリ
dig @ns-1864.awsdns-41.co.uk test.sub.hijiri0404.link A

# DNSキャッシュのクリア（ローカル環境）
sudo systemd-resolve --flush-caches  # Ubuntu/Debian
```

### 変更が反映されない
```bash
# Route53の変更ステータス確認
aws route53 get-change --id /change/C07823463K0IRIP5G2VIO

# TTL期間（300秒）待機後に再確認
```

---

## 📚 参考資料

- [AWS Route53 公式ドキュメント - サブドメイン委任](https://docs.aws.amazon.com/Route53/latest/DeveloperGuide/CreatingNewSubdomain.html)
- [RFC 1034 - Domain Names - Concepts and Facilities](https://www.rfc-editor.org/rfc/rfc1034)
- [RFC 1035 - Domain Names - Implementation and Specification](https://www.rfc-editor.org/rfc/rfc1035)

---

## ✨ まとめ

✅ **成功した項目:**
1. サブドメイン用ホストゾーン作成
2. NSレコードによる委任設定
3. テストAレコードの作成と解決確認（test.sub, aaa.sub, bbb.sub）
4. Zone Apex（sub.hijiri0404.link自体）のAレコード設定
5. DNSクエリの正常動作確認
6. 委任先でのレコード管理の実証

🎯 **達成目標:**
- `sub.hijiri0404.link` を独立したRoute53ホストゾーンで管理可能に
- 親ドメインとサブドメインの分離管理を実現
- DNS委任の仕組みを実証
- **Zone Apexのレコードはすべて委任先で管理することを確認**

---

**備考**: このドキュメントは実際の設定手順に基づいており、同様の委任設定を行う際の参照ガイドとして活用できます。
